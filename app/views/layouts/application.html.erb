<!DOCTYPE html>
<html>
<head>
	<link href="<%= image_path("favicon.png") %>" rel="icon" type="image/x-icon" />
	<!-- Set the viewport width to device width for mobile -->
	<meta name="apple-mobile-web-app-capable" content="yes">
	<meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no">
	<title>Whichbus Workshop</title>
	<%= stylesheet_link_tag    "application", :media => "all" %>
	<%= javascript_include_tag "application" %>
	<%= csrf_meta_tags %>
	<script type="text/javascript">

	var _gaq = _gaq || [];
	_gaq.push(['_setAccount', 'UA-28348026-4']);
	_gaq.push(['_trackPageview']);

	(function() {
	var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
	ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
	var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
	})();

</script>
</head>
<body>

<div class="navbar navbar-fixed-top">
	<div class="navbar-inner">
		<div class="container-fluid">
			<%= link_to workshop_path, class: 'brand' do %>
				WhichBus <span class='hide-phones'>WorkShop</span>
			<% end %>
			<form class="navbar-search form-inline pull-left" action="<%= workshop_path %>">
				<%= text_field_tag 'query', nil, class: "search-query span2", placeholder: 'search' %>
			</form>
			<button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<div class="nav-collapse collapse">
				<ul class="nav">
					<% %w[agencies routes stops].each do |model| %>
					<li <%= if model == params[:controller] then 'class=active' end %>>
						<%= link_to model.capitalize, "/workshop/#{model}" %>
					</li>
					<% end %>
				</ul>
			</div>
		</div>
	</div>
</div>
<div class="container-fluid">
<%= yield %>
</div>

</body>
</html>
