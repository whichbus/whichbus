Walk to <a class="btn btn-stop" <%= if @segment.to.stopId? then "href='/stops/#{@segment.to.stopId.agencyId}/#{@segment.to.stopId.id}'" %>><%= @segment.to.name %></a>
<span class="duration label"><%= Transit.format_time @segment.startTime %></span>
