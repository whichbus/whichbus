<div class="start<%= ' real-time' if @segment.real_time %>">Take <span class="badge badge-inverse"><%= @segment.route %></span></div>
<div class="finish">Get off at <%= @segment.to.name %> <span class="duration"><%= Math.round(@segment.duration/60000) %> minutes</span></div>
